<%= form_for recent,  html: { multipart: true } do |f| %>
  <% if recent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recent.errors.count, "error") %> prohibited this recent from being saved:</h2>

      <ul>
      <% recent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="field">
    <%= f.label :author %>
    <%= f.text_field :author %>
  </div>

  <div class="field">
    <%= f.label :photo %>
    <%= f.file_field :photo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
